// Top-level (project) build file - SCAFFOLD.
// Pins the Android Gradle Plugin + Kotlin versions. Verify these resolve for your Android Studio /
// JDK before trusting them (see README "Version compatibility"). AGP 8.7.x needs JDK 17.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
