# Evaluation harnesses. Heavier than the pipeline itself: the re-identification adversaries
# are PyTorch models and are trained/retrained here.
#
# TESTED PLATFORM -- Windows 11, Python 3.10.11, CPU + NVIDIA RTX 4060 (CUDA 12.4).
# The pins below are the versions this tree is currently verified against, so a failed
# reproduction differs from ours in data or configuration rather than in a library version.
# They are NOT a claim that every published number was measured under this exact set: the
# results span a longer period and some predate these versions. The continuous-integration
# job re-runs the parity and egress suites UNPINNED on ubuntu-latest / Python 3.10, which is
# what would surface a version-sensitive regression.
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.5.2
scikit-image==0.25.2
opencv-python==4.11.0.86
matplotlib==3.10.9
torch==2.6.0                 # CUDA build recommended; the adaptive (TM3) adversary is retrained.
                             # For CUDA 12.4 install from https://download.pytorch.org/whl/cu124
torchvision==0.21.0
lpips==0.1.4                 # perceptual distance for the visual-utility metrics
pytorch-fid==0.3.0           # Inception-pool3 features for FID (evaluation/quality/)
ultralytics==8.4.101         # YOLO11 seg/detect, used by the appearance harness and Tier-1 evals
onnxruntime==1.22.1
mediapipe==0.10.18
rtmlib==0.0.15
# ncnn==1.0.20260526   # Pi-5 only: evaluation/performance/pi_bench/bench_pi.py (aarch64 wheel)
# pycocotools          # detection AP/AR under the MS-COCO protocol
# Needed by `reproduce.py --run checkout`, the target documented as needing NO DATA. That is
# true of DATA and false of DEPENDENCIES, and the three below were not listed anywhere, so a
# clean environment failed the first target a reader runs. Found by installing this file into
# an empty virtualenv and running until it stopped failing, not by reading imports.
Pillow==12.3.0        # the AutoRef egress guard's image fixtures
requests==2.33.1      # tier1/src/mirage/encryption.py, for the TTP key fetch
cryptography==49.0.0  # the recovery-envelope crypto in the same module
